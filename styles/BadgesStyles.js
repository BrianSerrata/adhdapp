import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
carouselContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    paddingVertical: 10,
  },
  badgeIcon: {
    width: 150,
    height: 110,
    marginHorizontal: 8, // Adds spacing between badges
    resizeMode: "contain",
  },
  });

  export default styles